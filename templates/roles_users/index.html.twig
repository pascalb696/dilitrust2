{% extends 'base.html.twig' %}

{% block title %}RolesUsers index{% endblock %}

{% block body %}
    <h1>RolesUsers index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for roles_user in roles_users %}
            <tr>
                <td>{{ roles_user.id }}</td>
                <td>
                    <a href="{{ path('roles_users_show', {'id': roles_user.id}) }}">show</a>
                    <a href="{{ path('roles_users_edit', {'id': roles_user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('roles_users_new') }}">Create new</a>
{% endblock %}
