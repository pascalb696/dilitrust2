{% extends 'base.html.twig' %}

{% block title %}RolesUsers{% endblock %}

{% block body %}
    <h1>RolesUsers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ roles_user.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('roles_users_index') }}">back to list</a>

    <a href="{{ path('roles_users_edit', {'id': roles_user.id}) }}">edit</a>

    {{ include('roles_users/_delete_form.html.twig') }}
{% endblock %}
